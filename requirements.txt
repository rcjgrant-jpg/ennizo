# System packages not listed here: ffmpeg (record transcoding), installed
# via apt in the Dockerfile / Homebrew locally.

# --- web framework & serving ---
Django==5.2.15
gunicorn==26.2.0
whitenoise==6.12.0
psycopg[binary]==3.3.4
django-environ==0.14.0
python-dotenv==1.2.2

# --- background jobs ---
celery==5.6.3
redis==8.0.1

# --- media storage (S3 / Cloudflare R2) ---
django-storages==1.14.6
boto3==1.43.57

# --- audio analysis & processing ---
numpy==2.2.6
scipy==1.18.0
soundfile==0.14.0
librosa==0.11.0
numba==0.61.2       # librosa dependency
llvmlite==0.44.0    # numba's backend
essentia==2.1b6.dev1389   
pyloudnorm==0.2.0

# --- image uploads (avatar ImageField) ---
pillow==12.3.0

# --- tests ---
pytest==9.1.1
pytest-django==4.12.0